import { Locale } from "../interfaces/locale"

// locale : English
// author : Nick Payne

const en = {
    OK      : 'OK',
    CANCEL  : 'Cancel',
    CONFIRM : 'OK'
} satisfies Locale

export default en;